doctype html

html
	head
		meta(charset="utf8")
	body
		h1 BBS
		if loginUser
			a(href="/add-post") 发帖
			a(href="/logout") 登出
		else
			a(href="/login.html") 登陆
			a(href="/register.html") 注册
		block content