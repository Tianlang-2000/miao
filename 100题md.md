1. instanceof 运算符。
    ``` js
    function instanceOf(obj, constractor) {
      if (obj == null ||(typeof obj !== 'object' && typeof obj !== 'function')) {
        return false
      }

      let target = constractor.prototype
      let protoPointer = Obj.getPrototypeOf(obj)
      while(protoPointer !== null) {
        if (protoPointer === target) {
          return true
        }
        protoPointer = Obj.getPrototypeOf(protoPointer)
      }
      return false
    }
2. 手写new操作符。
    ``` js
	 function myNew(Constractor, ...args) {
		let obj = Object.create(Constractor.prototype)
    
		let result = Constractor.apply(obj, args)

		if (result !== null && (typeof result == 'object' || typeof result == 'function')) {
			return result
		}
		return obj
	 }
3. 手写apply函数
    ``` js
	 Function.prototype.myApply = function (thisArg, args) {
		if (typeof this !== 'function') {
			throw new typeError('must be called of function')
		}
		thisArg = thisArg == null ? globalThis : Object(thisArg)

		let fnKey = symbol('fn')
		thisArg[fnKey] = this
		const result = args ? thisArg[fnKey](...args) : thisArg[fnKey]()
		
		delete thisArg[fnKey]
		return result
	 }
4. 手写Object.create
    ``` js
	 function myObjectCreate(proto) {
		function f() {}
		f.prototype = proto
		return new f
	 }
5. call
    ``` js
	 Function.prototype.myCall = function (thisArgs, ...args) {
		return this.myApply(thisArgs,...args)
	 }
6. bind
    ``` js
	 Function.prototype.myBind = function (thisArg, ...presetArgs) {
		let fn = this

		function boundFn(laterArgs) {
			let isNew = this instanceof boundFn
			const finalObj = isNew ? this : thisArg
			return fn.apply(finalObj, [...presetArgs, ...laterArgs])
		}

		boundFn.prototype = Object.create(fn.prototype)

		return boundFn
	 }
7. Object.assign
    ``` js
	 Object.myAssign = function(target, ...sources) {
		if (target == null || target == undefined) {
			throw new TypeError('null不能转为对象')
		}
		let to = Object(target)

		for (let i = 0; i < sources.length; i++) {
			let source = sources[i]
			if(source == null || source == undifined) {
				continue
			}
			const keys = Object.keys(source)
			for (let key of keys) {
				to[key] = source[key]
			}

			const symbols = Object.getOwnPropertySymbols(source)
			for (let i = 0; i < symbols.length; i++) {
				const sym = symbols[i]

				if (source.propertyIsEnumerable(sym)) {
					to[sym] = source[sym]
				} 
			}
		}
		return to
	 }
8. Array.isArray
    ``` js
	  Array.myIsArray = function (value) {
		 const type = Object.prototype.toString.call(value)

		 return type === '[object Array]'
	  }
9. typeof better
    ``` js
		function betterType(value) {
			const type = Object.prototype.toString.call(value)
			const rawType = type.slice(8, -1)

			return rawType.toLowerCase()

		}
10. Proxy的get和set陷阱
    ``` js
		function createArray(arr) {
			return new Proxy(arr, {
				get(target, key) {
					const index = Number(key)

					if (index < 0) {
						return target[target.length + index]
					}

					return target[index]
				},
				set(target, key, value) {
					const index = Number(key)

					if (index < 0) {
						target[target.length + index] = value
						return true
					}

					target[key] = value
					return true
				}})
		}
11. 手写defineProperty和defineProperties
    ``` js
		function defineProperty(obj, key, descripter) {
			if (obj === null) {
				throw new TypeError('must be obj')
			}
			const pureDescripter = {}

			if ('value' in descripter) pureDescripter.value = descripter.value
			if ('writable' in descripter) pureDescripter.writable = !!descripter.writable

			if ('get' in descripter) pureDescripter.get = descripter.get
			if ('set' in descripter) pureDescripter.set = descripter.set

			if ('enumerable' in descripter) pureDescripter.enumerable = descripter.enumerable
			if ('configurable' in descriptor) pureDescriptor.configurable = !!descriptor.configurable;

			let hasData = !!pureDescripter.value || !!pureDescripter.writable
			let hasAccessor = 'get' in descripter || 'set' in descripter

			if (hasDate && hasAccessor) throw new TypeError('can not both specify accessor and data descripter')
      
			Object.defineProperty(obj, key, pureDescripter)

			return obj
		} 
		function defineProperties(obj, props) {
			if (obj === null || obj === undefined) {
				throw new TypeError('must obj')
			}
			if (typeof props !== 'object' || props === null) {
				throw new TypeError('must obj')
			}
			const O = Object(obj)
			const descripts = Object(props)
			
			const keys = Object.keys(props)

			for (let i = 0; i < keys.length; i++) {
				let key = keys[i]
				let descript = descripts[i]
				Object.defineProperty(o, key, descript)
			}
			return o
		}
12. 手写Object.freeze浅冻结
    ``` js
		function shallowFreeze(obj) {
			if (typeof obj !== 'object' || obj == null) {
				return obj
			}

			Object.preventExtensions(obj)

			let nameProps = Object.getOwnPropertyNames(obj)
			let symProps = Object.getOwnPropertySymbols(obj)
			const allProps = [...nameProps, symProps]

			allProps.forEach((prop) => {
				let descripter = Object.getOwnPropertyDescripter(obj, prop)

				if(descripter) {
					descripter.configerable = false

					if (Object.prototype.hasOwnProperty.call(descripter, 'value')) {
						descripter.writable = false
					}
					Object.defineProperty(obj, prop, descripter)
				}


			})

			return obj
		}
13. 手写Object.seal密封对象 禁止添加删除属性，但可修改现有属性。
    ``` js
		function shallowFreeze(obj) {
			if (typeof obj !== 'object' || obj == null) {
				return obj
			}

			Object.preventExtensions(obj)

			let nameProps = Object.getOwnPropertyNames(obj)
			let symProps = Object.getOwnPropertySymbols(obj)
			const allProps = [...nameProps, symProps]

			allProps.forEach((prop) => {
				let descripter = Object.getOwnPropertyDescripter(obj, prop)

				if(descripter) {
					descripter.configerable = false
					Object.defineProperty(obj, prop, descripter)
				}
			})

			return obj
		}
14. 手写getter和setter拦截 实现对象属性的读写拦截。
    ``` js
		function defineReactive(obj, key, val) {
			if (typeof obj == 'object' && obj !== null) {
				observer(obj)
			}

			Object.defineProperty(obj, key, {
				enumable: true,
				configurable: true,
				get() {
					console.log(`${key} = ${val}`)
					//
					return val
				},
				set(newVal) {
					if (newVal == val) return
					console.log(`${key} = ${newVal}, 旧值${val}`)
					val = newVal
					// 
				}
			})
		}
		function observer(obj) {
			if (typeof obj !== 'object' || obj == null) return
			Object.keys(obj).forEach((key) => {
				defineReactive(obj, key, obj[key])
			})
		}

		function reactive(target) {
			if(typeof target !== 'object' && target == null) {
				return target
			}
			const handler = {
				get(target, key, receiver) {
					const value = Reflect.get(target, key, receiver)
					console.log(`🎯 读取属性: ${key} = ${value}`)
					if (typeof value == 'object' && value !== null) {\
						return reactive(value)
					}
					return value
				},
				set(target, key, newVal, receiver) {
					const oldValue = target[key]
					const result = Reflect.set(target, key, newVal, receiver)
					if (newVal !== oldValue) {
						console.log(`设置${key}属性值为${newVal} , 旧值为${oldValue}`)
					}
					return result
				},
				deleteProperty(target, key) {
					const hasKey = key in target
					const result = Reflect.deleteProperty(target, key)

					if (hasKey && result) {
        		console.log(`❌ 删除属性: ${key}`);
      		}
					return result
				},
				has(target, key) {
      		console.log(`🔍 检查属性: ${key}`);
      		return Reflect.has(target, key);
    		}
			}

			return new Proxy(target, handler)
		}
15. 手写Array.of创建一个新数组实例，无论参数数量或类型。
    ``` js
		function myArrayOf() {
			const arr = new Array(arguments.length)
			
			for (let i = 0; i < arguments.length; i++) {
				arr.push(arguments[i])
			}

			return arr
		}

16. 手写Array.from 将类数组对象或可迭代对象转为数组。
    ``` js
		function myArrayFrom(arrayLike, mapFn, thisArgs) {
			if (arrayLike == null) {
				throw new TypeError('')
			}

			const arr = []

			const hasIterator = typeof arrayLike[symbol.iterator] == 'function'

			if (hasIterator) {
				for (let item of arrayLike) {
					arr.push[item]
				}
			} else {
				const len = arrayLike.length
				for (let i = 0; i < len; i++) {
					if (i in arrayLike) {
						arr.push(arrayLike[i])
					} else {
						arr.push(undefined)
					}
				}
			}

			if (mapFn !== null) {
				if (typeof mapFn !== 'function') {
					throw new TypeError('')
				}
				for (let i = 0; i < arr.length; i++) {
					arr[i] = mapFn.call(thisArgs, arr[i], i)
				}
			}

			return arr
		}
17. 手写迭代器实现fibonacci 实现斐波那契数列的迭代器。
    ``` js
		function* fibonacciGenerator(maxCount = Infinity) {
			let n1 = 0
			let n2 = 0
			let count = 0
			
			while (count < maxCount) {
				yield n1
				[n1, n2] = [n2, n1+ n2]
				count++
			}
		}
18. 手写生成器函数range 实现生成指定范围的数字序列。
    ``` js
		function* range(start, end, step = 1) {
			if (end === undefined) {
				end = start
				start = 0
			}
			if (step === 0) {
				throw new TypeError('')
			}

			if (step > 0) {
				for (let i = start; i < end; i += step) {
					yield i
				}
			} else {
				for (let i = end; i < start; i += step) {
					yield i
				}
			}
		}
19. 手写模块加载器require简易实现CommonJS模块加载机制。
    ``` js
		const moduleCache = {}

		function myRequire(modulePath) {
			const fullPath = resolveModulePath(modulePath)
			if (moduleCache[fullPath]) {
				return moduleCache[fullPath].exports
			}

			const module = {
				exports: {},
				id: fullPath,
				loaded: false
			}

			moduleCache[fullPath] = module

			try {
				const moduleCode = mockModules[fullPath]

				if (!moduleCode) {
					throw new Error('can not find module')
				}

				const context = {
					exports: module.exports,
					module,
					require: myRequire,
					__dirname: getDirname(fullPath),
					__filename: fullPath
				}

				const wrapper = `(function(exports, module, require, __dirname, __filename) {\n${moduleCode}\n})`
				const compiled = eval(wrapper)

				compiled.call(
     		  context.exports,
      		context.exports,
      		context.module,
      		context.require,
      		context.__dirname,
      		context.__filename
    		)
				module.loaded = true

				return module.exports
			} catch (error) {
    		delete moduleCache[fullPath];
    		throw error;
  		}

		}
20. 手写ES6 Class转ES5模拟class语法糖的底层实现。
    ``` js

21. 手写extends继承
实现类的继承机制，包括静态方法继承。
1.  手写mixin混入
实现多继承的混入模式。
1.  手写deepClone深拷贝
完整实现对象深度克隆，处理循环引用、Symbol、Date、RegExp等。
1.  手写shallowClone浅拷贝
实现对象的浅层复制。
1.  手写extend函数
类似jQuery.extend，支持深浅拷贝。
1.  手写curry柯里化函数
将多参数函数转为单参数函数链式调用。
1.  手写compose函数组合
从右到左组合多个函数。
1.  手写pipe函数管道
从左到右组合多个函数。
1. 手写debounce防抖,事件触发后等待n秒再执行，期间再次触发重新计时。
   ``` js
	 function debounce(func, delay) {
		let timer = null
		return function(...args) {
			if (timer) {
				clearTimeout(timer)
			}
			timer = setTimeout(() => {
				func.apply(this, args)
			}, delay)
		}
	 }
2.  手写throttle节流 限制函数执行频率，n秒内最多执行一次。
    ``` js
		function throttle(fn, wait) {
			let lastTime = 0
			return function () {
				const now = new Date().getTime()
				if ((now - lastTime) >= wait) {
					fn()
					lastTime = now
				}
			}
		}
二、数组操作篇（15题）
1.  手写Array.prototype.flat
实现数组扁平化，支持指定深度。
1.  手写Array.prototype.flatMap
先map再flat的深度为1的扁平化。
1.  手写数组去重unique
多种方法实现：Set、Map、filter+indexOf等。
1.  手写数组shuffle洗牌
实现Fisher-Yates洗牌算法，保证随机性。
1.  手写sample随机采样
从数组中随机取一个或多个元素。
1.  手写chunk数组分块
将数组分割成指定大小的多个块。
1.  手写zip数组拉链
合并多个数组的元素。
1.  手写union数组合并
合并多个数组并去重。
1.  手写intersection数组交集
求多个数组的交集。
1.  手写difference数组差集
求在A但不在B中的元素。
1.  手写pull移除元素
移除数组中所有匹配的元素。
1.  手写remove条件移除
根据条件函数移除元素并返回移除项。
1.  手写sortBy多条件排序
根据多个字段进行排序。
1.  手写groupBy分组
根据条件函数将数组元素分组。
1.  手写countBy计数统计
统计数组中各类元素出现次数。
三、字符串处理篇（10题）
1.  手写字符串reverse
原地反转字符串（不可使用内置reverse）。
1.  手写palindrome回文判断
判断字符串是否为回文。
1.  手写truncate截断
按指定长度截断字符串，添加省略号。
1.  手写camelCase转驼峰
将各种命名字符串转为驼峰命名。
1.  手写kebabCase转短横线
转为短横线命名。
1.  手写template字符串模板
实现${}模板字符串替换。
1.  手写queryString解析
将URL参数解析为对象。
1.  手写queryString序列化
将对象序列化为URL参数。
1.  手写escapeHTML转义
防止XSS攻击的HTML转义。
1.  手写unescapeHTML反转义
四、Promise异步篇（10题）
1.  手写Promise类核心
实现Promise的then、catch、resolve、reject基础功能。
1.  手写Promise.all
全部成功才成功，一个失败即失败。
1.  手写Promise.race
竞速，第一个完成的结果。
1.  手写Promise.allSettled
等待所有promise完成，不管成功失败。
1.  手写Promise.any
任意一个成功即成功，全部失败才失败。
1.  手写Promise.withResolvers
创建Promise并返回resolve/reject函数。
1.  手写Promise.retry
失败重试机制，支持重试次数和延迟。
1.  手写Promise.map带并发控制
控制并发数的异步map操作。
1.  手写Promise.waterfall
串行执行promise数组。
1.  手写async/await原理
用generator和promise模拟async/await。
五、DOM与BOM篇（8题）
1.  手写原生AJAX请求
完整实现GET、POST、PUT、DELETE等。
1.  手写JSONP跨域
动态创建script标签实现跨域请求。
1.  手写事件委托
利用事件冒泡实现代理机制。
1.  手写EventEmitter
发布订阅模式，支持on、emit、off、once。
1.  手写localStorage封装
带过期时间、容量检测的增强版。
1.  手写Cookie操作
封装setCookie、getCookie、removeCookie。
1.  手写DOM选择器
简易实现querySelectorAll功能。
1.  手写$选择器
模拟jQuery的Sizzle选择器核心。
六、CSS手写篇（8题）
1.  手写CSS水平垂直居中（5种方法）
flex、grid、absolute+transform、table-cell、line-height等。
1.  手写CSS画三角形
利用border属性绘制各种方向的三角形。
1.  手写CSS三栏布局（圣杯、双飞翼）
实现中间自适应，两侧固定宽度。
1.  手写CSS两栏布局
左固定右自适应或右固定左自适应。
1.  手写clearfix清除浮动
解决父元素高度塌陷问题。
1.  手写BFC创建
触发块级格式化上下文的方法。
1.  手写CSS动画（keyframes）
实现淡入淡出、旋转、缩放等基础动画。
1.  手写CSS变量主题切换
使用CSS变量实现一键换肤。
七、算法与数据结构篇（10题）
1.  手写斐波那契数列（递归、迭代、动态规划）
三种实现方式，优化时间复杂度。
1.  手写阶乘函数
递归与非递归实现。
1.  手写二分查找
在有序数组中查找目标值。
1.  手写快速排序
原地快排实现。
1.  手写归并排序
稳定排序算法。
1.  手写冒泡排序与优化
基础排序与提前退出优化。
1.  手写TOP K问题
求数组中最大的K个数，堆排序实现。
1.  手写LRU缓存
实现最近最少使用缓存淘汰算法。
1.  手写判断链表相交
判断两个单向链表是否相交并找出交点。
1.  手写反转链表
原地反转单向链表。
1.  手写Two Sum两数之和
找出数组中和为目标值的两个数。
八、工具函数篇（9题）
1.  手写get路径获取
根据路径'a.b.c'从对象中取值。
1.  手写set路径设置
根据路径设置对象深层属性值。
1.  手写cloneDeep完整版
处理循环引用、Symbol、Map、Set、Date、RegExp等。
1.  手写isEqual深度比较
比较两个值是否相等，支持引用类型。
1.  手写flatten扁平化
将嵌套数组转为扁平数组。
1.  手写unflatten反扁平化
将扁平对象转为嵌套对象。
1.  手写sleep延迟函数
实现Promise版本的延迟函数。
1.   手写typeCheck类型检查
完整类型判断，包括isPlainObject、isEmpty等。